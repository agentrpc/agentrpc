/**
 * # AgentRPC
 *
 * ## Installation
 *
 * ```bash
 * npm install agentrpc
 * ```
 *
 * ```bash
 * yarn add agentrpc
 * ```
 *
 * ```bash
 * pnpm add agentrpc
 * ```
 */

export { AgentRPC } from "./agentrpc";
