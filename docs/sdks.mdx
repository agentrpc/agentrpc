---
title: 'SDKs'
description: 'AgentRPC SDK documentation for multiple languages'
---

# AgentRPC SDKs

AgentRPC provides SDKs for multiple programming languages, making it easy to integrate with your existing codebase. Each SDK allows you to register functions as tools that can be called by AI agents.

## Available SDKs

<CardGroup cols={3}>
  <Card
    title="TypeScript/JavaScript"
    icon="js"
    href="#typescript-sdk"
  >
    For Node.js applications
  </Card>
  <Card
    title="Go"
    icon="golang"
    href="#go-sdk"
  >
    For Go applications
  </Card>
  <Card
    title="Python"
    icon="python"
    href="#python-sdk"
  >
    For Python applications
  </Card>
</CardGroup>

## TypeScript SDK

The TypeScript SDK provides a simple way to register functions as tools for AI agents.

### Installation

```bash
npm install agentrpc
```

### Basic Usage

```typescript
import { AgentRPC } from "agentrpc";
import { z } from "zod";

const client = new AgentRPC({
  apiSecret: "YOUR_API_SECRET",
});

client.register({
  name: "hello",
  schema: z.object({ name: z.string() }),
  handler: async ({ name }) => `Hello ${name}`,
  // Optional configuration
  config: {
    retryCountOnStall: 3,
    timeoutSeconds: 30,
  },
});

await client.listen();
```

### OpenAI Integration

```typescript
// Get tools formatted for OpenAI
const tools = await client.OpenAI.getTools();

// Pass these tools to OpenAI
const chatCompletion = await openai.chat.completions.create({
  model: "gpt-4o",
  messages: messages,
  tools: tools,
  tool_choice: "auto",
});

// Execute tool calls from OpenAI's response
if (responseMessage.tool_calls && responseMessage.tool_calls.length > 0) {
  for (const toolCall of responseMessage.tool_calls) {
    const result = await client.OpenAI.executeTool(toolCall);
    // Use the result...
  }
}
```

### MCP Server

The TypeScript SDK includes an MCP (Model Context Protocol) server that can be started using:

```bash
npx agentrpc mcp <YOUR_API_SECRET>
```

For more details, visit the [TypeScript SDK GitHub repository](https://github.com/agentrpc/agentrpc/tree/main/sdk-node).

## Go SDK

The Go SDK provides a clean interface for registering functions as tools for AI agents.

### Installation

```bash
go get github.com/agentrpc/agentrpc/sdk-go
```

### Basic Usage

```go
package main

import (
  "context"
  "fmt"

  "github.com/agentrpc/agentrpc/sdk-go"
)

func main() {
  client, err := agentrpc.New(agentrpc.Options{
    APISecret: "YOUR_API_SECRET",
  })
  if err != nil {
    panic(err)
  }

  err = client.Register(agentrpc.Tool{
    Handler: func(input map[string]interface{}) (interface{}, error) {
      name, ok := input["name"].(string)
      if !ok {
        return nil, fmt.Errorf("name must be a string")
      }
      return fmt.Sprintf("Hello %s", name), nil
    },
    Name:        "SayHello",
    Description: "A simple greeting function",
  })
  if err != nil {
    panic(err)
  }

  err = client.Listen()
  if err != nil {
    panic(err)
  }

  // To stop listening
  // client.Unlisten()
}
```

For more details, visit the [Go SDK GitHub repository](https://github.com/agentrpc/agentrpc/tree/main/sdk-go).

## Python SDK

The Python SDK provides a Pythonic way to register functions as tools for AI agents.

### Installation

```bash
pip install agentrpc
```

### Basic Usage

```python
from agentrpc import AgentRPC

client = AgentRPC(api_secret="YOUR_API_SECRET")

@client.register
def get_weather(location: str) -> dict:
    """Get the weather for a specific location."""
    # Implementation details
    return {"temperature": 72, "condition": "sunny", "location": location}

client.listen()
```

### OpenAI Integration

```python
from agentrpc import AgentRPC
from openai import OpenAI

openai = OpenAI(api_key="YOUR_OPENAI_API_KEY")
agentrpc = AgentRPC(api_secret="YOUR_API_SECRET")

# Get tools formatted for OpenAI
tools = agentrpc.openai.get_tools()

# Use tools with OpenAI
completion = openai.chat.completions.create(
    model="gpt-4o",
    messages=[{"role": "user", "content": "What's the weather in San Francisco?"}],
    tools=tools,
)

# Execute tool calls
if completion.choices[0].message.tool_calls:
    for tool_call in completion.choices[0].message.tool_calls:
        result = agentrpc.openai.execute_tool(tool_call)
        # Use the result...
```

For more details, visit the [Python SDK GitHub repository](https://github.com/agentrpc/agentrpc/tree/main/sdk-python).

## Coming Soon

<Card
  title=".NET SDK"
  icon="microsoft"
>
  Support for .NET applications is coming soon.
</Card> 